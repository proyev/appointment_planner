import React from "react";

export const TileList = props => {
  const renderData = () => {
    for (const obj of props.toRender) {

    }
  }

  return (
    <div>
      {}
    </div>
  );
};
